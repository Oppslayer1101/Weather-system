# Weather-system
A simple Python machine learning project that predicts temperature using humidity and pressure values. It loads data from a CSV file, trains a Linear Regression model, and allows users to input values to get a predicted temperature. Demonstrates basic AIML and Python concepts.
